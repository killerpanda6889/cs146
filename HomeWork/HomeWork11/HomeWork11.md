For both languages we check if the color we want to update is present,
then we move the 4 directions and add the coordinates to an array then to a q
and move depth first till we reach all nodes, if a node is updated we dont touch
Theta(n^2)